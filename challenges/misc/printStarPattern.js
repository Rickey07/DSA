// Write a function in JavaScript to print the following right-angled triangle star pattern using asterisks (*) for a given number n.

function printStarPattern (n) {
    if(n ===  0) return null
    // for(let i=1 ; i<=n ; i++) {
    //     str+="*"
    //     console.log(str);
    // }

    // for(let i = n ; i>=1 ; i--) {
    //     let str = ""
    //     for(let j = 1 ; j<=i ; j++) {
    //         str+="*"
    //     }
    //     console.log(str)
    // }
}

printStarPattern(5)